# Top-level env.example for both frontend and backend
# Copy values into a local `.env` (or into each service's env) as needed.

# -------------------------
# Frontend (Vite) variables
# Prefix with `VITE_` so Vite exposes them to the client.
# -------------------------
VITE_APPWRITE_ENDPOINT=
VITE_APPWRITE_PROJECT_ID=
VITE_CLERK_PUBLISHABLE_KEY=

# -------------------------
# Backend / API server variables
# (used in backend/internal/config/config.go)
# -------------------------
PORT=
APPWRITE_ENDPOINT=
APPWRITE_PROJECT_ID=
APPWRITE_API_KEY=
APPWRITE_DATABASE_ID=
APPWRITE_USERS_COLLECTION_ID=
APPWRITE_POSTS_COLLECTION_ID=
APPWRITE_COMMENTS_COLLECTION_ID=
APPWRITE_LIKES_COLLECTION_ID=
APPWRITE_MEDIA_COLLECTION_ID=
APPWRITE_STORAGE_BUCKET_ID=

# OAuth / third-party credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=
GITHUB_REDIRECT_URL=

# Auth
JWT_SECRET=
ADMIN_EMAIL=
ADMIN_PASSWORD=

# -------------------------
# Server (server/ subproject) DB variables
# (used in server/internal/database/database.go)
# -------------------------
BLUEPRINT_DB_DATABASE=
BLUEPRINT_DB_USERNAME=
BLUEPRINT_DB_PASSWORD=
BLUEPRINT_DB_HOST=
BLUEPRINT_DB_PORT=

# Notes:
# - A service-specific `.env.example` already exists at backend/.env.example
#   This top-level file consolidates all keys used by frontend, backend, and
#   the `server/` subproject for convenience.
# - Never commit real secrets; keep the real `.env` out of version control.